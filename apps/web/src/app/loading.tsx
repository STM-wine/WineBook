import { WineLoadingProgress } from "@/components/wine-loading-progress";

export default function Loading() {
  return <WineLoadingProgress message="Loading" detail="Preparing dashboard data." />;
}
