# Archived settings pages

These pages are intentionally preserved but are no longer part of the active Settings navigation or runtime. Their former public routes redirect to the Settings overview so old bookmarks do not load the archived database queries.

- Gross Profit Center
- Laid-In Coverage
- QB Vendors
- History

QuickBooks Item Diagnostics is archived separately in `components/_archive`, with its former summary endpoint under `app/api/_archive`. Neither is linked or registered as an active route.
